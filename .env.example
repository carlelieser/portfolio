# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com
# Navigate to: Project Settings > API

# PUBLIC_SUPABASE_URL - Your Supabase project URL
# Format: https://[project-ref].supabase.co
# This is safe to expose to the client (hence the PUBLIC_ prefix)
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# PUBLIC_SUPABASE_ANON_KEY - Supabase anonymous/public key
# This key is safe to use in client-side code
# RLS policies restrict what this key can access
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# SUPABASE_SERVICE_ROLE_KEY - Supabase service role key (PRIVATE)
# ⚠️ CRITICAL: This key bypasses ALL Row-Level Security policies
# NEVER expose this key to the client or commit it to version control
# Only use in server-side code (API routes, server hooks)
# Used for: Webhook handlers, admin operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Patreon Configuration
# Get these from your Patreon app: https://www.patreon.com/portal/registration/register-clients

# Patreon Webhook Secret - Used to verify webhook requests
PATREON_WEBHOOK_SECRET=your-patreon-webhook-secret

# Patreon OAuth Credentials
PATREON_CLIENT_ID=your-patreon-client-id
PATREON_CLIENT_SECRET=your-patreon-client-secret

# Patreon Creator's Access Token (for API calls on behalf of your campaign)
PATREON_CREATOR_ACCESS_TOKEN=your-creator-access-token
PATREON_CREATOR_REFRESH_TOKEN=your-creator-refresh-token
